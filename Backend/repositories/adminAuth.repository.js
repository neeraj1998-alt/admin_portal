const findAdminByEmail = async (email) => {
  // TODO: replace with actual database query once the admin schema is available.
  // Example intent: SELECT * FROM admin_users WHERE email = ?
  return null;
};

const findAdminById = async (id) => {
  // TODO: replace with actual database query once the admin schema is available.
  // Example intent: SELECT * FROM admin_users WHERE id = ?
  return null;
};

module.exports = {
  findAdminByEmail,
  findAdminById,
};
